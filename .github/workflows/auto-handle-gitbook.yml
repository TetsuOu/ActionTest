name: auto-handle-gitbook
on: 
  push:
    branches:
    - main

jobs:
  main-to-gh-pages:
    runs-on: ubuntu-latest
        
    steps:
    - name: checkout main
      uses: actions/checkout@v2
      with:
        ref: main
            
    - name: install nodejs
      uses: actions/setup-node@v1
      
    - name: configue gitbook
      run: |
        npm install -g gitbook-cli
        gitbook install
        npm install -g gitbook-summary
                
    - name: generate _book
      run: |
        book sm
        gitbook build
                
    - name: push _book to gh-pages
      env:
        TOKEN: ${{ secrets.TOKEN }}
        REF: github.com/wangzhebufangqi/ActionTest.git
        MYEMAIL: 1823636309@qq.com
        MYNAME: ${{github.actor}}           #The login of the user that initiated the workflow run.
      run: |
        echo ${{github.workspace}}
        cd _book
        git config --global user.email "${MYEMAIL}"
        git config --global user.name "${MYNAME}"
        git init
        git remote add origin https://${REF}
        git add . 
        git commit -m "Updated By Github Actions With Build ${{github.run_number}} of ${{github.workflow}} For Github Pages"
        git branch -M main
        git push --force --quiet "https://${TOKEN}@${REF}" main:gh-pages
                
                
                